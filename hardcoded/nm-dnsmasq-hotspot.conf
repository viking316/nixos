# NetworkManager dnsmasq configuration for hotspot
# This ensures DHCP works properly in AP mode

# Increase log level for debugging (remove this later)
log-dhcp

# Bind to the interface and enable DHCP
bind-interfaces

# Set authoritative mode for DHCP
dhcp-authoritative

# Reduce DHCP timeout
dhcp-rapid-commit
